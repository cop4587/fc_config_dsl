COMMON

X fc_config works in production mode
X mix conf and data (modify and creation)

X mix IMBS with IMAS - functional tested
X mix IMAS with IMBS - functional tested

. various error conditions handling

. copy no change files to output
  - only one group is allowed to be modified at one time,
    thus output contains that group's files only, so no group folder needed.

YAML

X modify: add / upd / del
X create: add
X output/e<n>/
X re-open module
X module_support

X project structure
  X platform_descriptor/
  X lib/
  X bin/

X platform_descriptor/e<n>/e<n>.rb

X create gem
X add unit test

CONF

X modify: add / upd / del
X create: add
X output/e<e>
X re-open module

REFACTORING

. module ConfUtil has duplication with module YamlUtil